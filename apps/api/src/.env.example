# Database
DATABASE_URL=postgresql://user:password@localhost:5432/betintel

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_REFRESH_SECRET=

# Firebase (backend) — base64-encoded service account JSON
FIREBASE_SERVICE_ACCOUNT_JSON=

# Redis
REDIS_URL=redis://localhost:6379

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=BetIntel <noreply@betintel.app>

# App
PORT=3000
NODE_ENV=development