node_modules/
.expo/
.eas/
android/
ios/
/dist/
build/
*.log

# API directory — only the mobile app is needed for EAS builds
../api/
../../.github/
# Note: ../../packages/ must stay included — pnpm needs packages/shared to resolve workspace:* deps