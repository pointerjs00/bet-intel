name: Build & Deploy API

on:
  push:
    branches: [main]
    paths: ['apps/api/**', 'packages/shared/**']
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}/betintel-api:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download API image
        run: |
          docker pull ghcr.io/${{ github.repository }}/betintel-api:latest
          docker save ghcr.io/${{ github.repository }}/betintel-api:latest | gzip > "$RUNNER_TEMP/betintel-api.tar.gz"

      - name: Create deploy env file
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          APP_URL: ${{ secrets.APP_URL }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
          LOG_LEVEL: ${{ secrets.LOG_LEVEL }}
        run: |
          cat > "$RUNNER_TEMP/api.env" <<EOF
          DATABASE_URL=${DATABASE_URL}
          JWT_SECRET=${JWT_SECRET}
          JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
          REDIS_URL=${REDIS_URL}
          FIREBASE_SERVICE_ACCOUNT_JSON=${FIREBASE_SERVICE_ACCOUNT_JSON}
          SMTP_HOST=${SMTP_HOST}
          SMTP_PORT=${SMTP_PORT}
          SMTP_USER=${SMTP_USER}
          SMTP_PASS=${SMTP_PASS}
          SMTP_FROM=${SMTP_FROM}
          APP_URL=${APP_URL}
          ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
          PORT=3000
          NODE_ENV=production
          LOG_LEVEL=${LOG_LEVEL:-info}
          EOF

      - name: Upload Docker Compose file
        env:
          HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
          HETZNER_USER: ${{ secrets.HETZNER_USER }}
          HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
        run: |
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$HETZNER_SSH_KEY" > "$HOME/.ssh/hetzner_key"
          chmod 600 "$HOME/.ssh/hetzner_key"
          ssh -i "$HOME/.ssh/hetzner_key" -o StrictHostKeyChecking=no "$HETZNER_USER@$HETZNER_HOST" 'mkdir -p ~/betintel/apps/api ~/betintel/images'
          scp -i "$HOME/.ssh/hetzner_key" -o StrictHostKeyChecking=no apps/api/docker-compose.yml "$HETZNER_USER@$HETZNER_HOST:~/betintel/apps/api/docker-compose.yml"
          scp -i "$HOME/.ssh/hetzner_key" -o StrictHostKeyChecking=no "$RUNNER_TEMP/api.env" "$HETZNER_USER@$HETZNER_HOST:~/betintel/apps/api/.env"
          scp -i "$HOME/.ssh/hetzner_key" -o StrictHostKeyChecking=no "$RUNNER_TEMP/betintel-api.tar.gz" "$HETZNER_USER@$HETZNER_HOST:~/betintel/images/betintel-api.tar.gz"

      - name: Deploy to Hetzner via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: ${{ secrets.HETZNER_USER }}
          key: ${{ secrets.HETZNER_SSH_KEY }}
          script: |
            export GITHUB_REPO=${{ github.repository }}
            COMPOSE_FILE="$HOME/betintel/apps/api/docker-compose.yml"
            ENV_FILE="$HOME/betintel/apps/api/.env"
            IMAGE_ARCHIVE="$HOME/betintel/images/betintel-api.tar.gz"
            gunzip -c "$IMAGE_ARCHIVE" | docker load
            if command -v docker-compose >/dev/null 2>&1; then
              docker-compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d --no-deps api
            else
              docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" up -d --no-deps api
            fi